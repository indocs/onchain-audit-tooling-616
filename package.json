{
  "name": "onchain-audit-tooling",
  "version": "0.1.0",
  "private": true,
  "description": "Automates pre-flight smart contract audits with on-chain regressions, enabling continuous verification of reentrancy, overflow, and access-control patterns.",
  "type": "module",
  "scripts": {
    "format": "prettier --write '**/*.{ts,sol,json}'",
    "build": "tsc -p tsconfig.json",
    "test": "hardhat test",
    "deploy": "ts-node ./scripts/deploy.ts --network "$NETWORK""
  },
  "dependencies": {
    "ethers": "^5.7.2"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^2.1.0",
    "ethers": "^5.7.2",
    "hardhat": "^3.0.0",
    "ts-node": "^10.9.1",
    "typescript": "^5.3.0",
    "dotenv": "^16.0.3",
    "@types/node": "^18.19.0"
  }
}